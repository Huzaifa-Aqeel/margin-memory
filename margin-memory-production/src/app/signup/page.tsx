import Link from 'next/link'
import { signupAction } from '@/app/actions/auth'
export default async function SignupPage({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <div className="auth-page"><div className="auth-card"><div className="auth-brand"><span className="brand-mark">M</span><div><strong>Margin Memory</strong><span>Built for owner-estimators.</span></div></div><h1>Create your account</h1><p className="subtle">Your jobs, estimates, and company memory stay in your private workspace.</p>{p.error&&<div className="error">{p.error}</div>}<form action={signupAction} className="auth-form"><div className="field"><label>Work email</label><input type="email" name="email" autoComplete="email" required/></div><div className="field"><label>Password</label><input type="password" name="password" minLength={8} autoComplete="new-password" required/><div className="helper">At least 8 characters.</div></div><button className="btn primary" type="submit">Create account</button></form><div className="auth-switch">Already have an account? <Link href="/login"><strong>Sign in</strong></Link></div></div></div>}
