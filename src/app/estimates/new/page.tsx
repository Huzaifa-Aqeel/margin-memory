import { UploadEstimateForm } from '@/components/upload-estimate-form'
export default async function NewEstimatePage({searchParams}:{searchParams:Promise<{revisionOf?:string}>}){const{revisionOf}=await searchParams;return <div className="page"><div className="page-head"><div><div className="eyebrow">{revisionOf?'Estimate revision':'New Margin Check'}</div><h1>{revisionOf?'Review a revised estimate':'Review an estimate'}</h1><p className="subtle">Keep estimating in your current tool. Upload the finished XLSX/CSV and Margin Check will identify company-history risks worth another look.</p></div></div><UploadEstimateForm parentEstimateId={revisionOf}/></div>}
